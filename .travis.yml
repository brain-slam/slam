language: python
python:
  - "2.7"
  - "3.6"
# command to install dependencies
install:
  - pip install flake8
  - pip install -r requirements.txt
# command to run tests
script:
  - python -c "import slam"
  - pytest
  - flake8 slam/